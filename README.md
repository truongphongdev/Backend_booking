# 1: Dự án backend với express

## 2: Công cụ

    - Express JS => chạy chính
    - dotenv => giấu thông  tin cần che
    - nodemon => chạy lại khi lắng nghe thay đổi
    - mysql2 => DB
    - sequelize => orm
    - sequelize-cli => chạy lệnh sequelize
    - cors => dùng để fig lỗi khi fe call api
